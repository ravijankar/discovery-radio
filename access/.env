# Required
ADMIN_PASSWORD=hal9000

# Optional — defaults shown
PORT=3001
DB_PATH=invites.db

# AzuraCast stream URL (set after AzuraCast is configured)
# This is the stream URL that listeners will connect to — token is appended as ?token=
# Example: https://stream.ravijankar.com/radio
STREAM_URL=
